import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'theming-showcase',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
